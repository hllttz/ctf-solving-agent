# Solver Skills

Drop optional `.md` skill files in this directory to add challenge-specific guidance to the solver prompt.

Keep each skill concise and action-oriented. A skill should describe when it applies, what to try first, and which tools or commands are useful. The solver loads all markdown files in lexical order.
